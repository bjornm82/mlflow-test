name: tutorial

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      alpha: float
      l1_ratio: {type: float, default: 0.1}
    command: "papermill train.ipynb output.ipynb -p {alpha} -p {l1_ratio}"
